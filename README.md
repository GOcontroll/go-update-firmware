# go-update-firmware

A small program that flashes new firmware to the currently inactive boot  
partition and then sets that partition as active.

Heavily inspired by the mmcboot handler in Barebox and mmc-utils
